import React from "react";
import Paper from "@material-ui/core/Paper";
import Style from "../utils/style.js";

export default function TopErrors({ data }) {
  const style = Style();
  return <Paper className={style.topErrors}>lista</Paper>;
}
